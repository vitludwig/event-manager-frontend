import { TransformWidthPipe } from './transform-width.pipe';

describe('TransformWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
