import { EventEllipsisPipe } from './event-ellipsis.pipe';

describe('EventEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new EventEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
