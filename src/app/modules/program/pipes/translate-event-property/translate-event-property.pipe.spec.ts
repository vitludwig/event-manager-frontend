import { TranslateEventPropertyPipe } from './translate-event.pipe';

describe('TranslateEventPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateEventPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
