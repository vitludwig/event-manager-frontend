import { TranslateNotificationPropertiesPipe } from './translate-notification-properties.pipe';

describe('TranslateNotificationPropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateNotificationPropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
