import { AutoUppercaseDirective } from './auto-uppercase.directive';

describe('AutoUppercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoUppercaseDirective();
    expect(directive).toBeTruthy();
  });
});
